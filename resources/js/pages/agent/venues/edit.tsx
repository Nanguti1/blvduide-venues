import AppLayout from '@/layouts/app-layout';

export default function AgentVenueEdit() {
  return (
    <AppLayout>
      <div className="p-6">Venue edit form scaffolded (React Hook Form integration next).</div>
    </AppLayout>
  );
}
